import React from 'react';

import { Alert } from 'reactstrap';

const PageNotFound = () => {
  return (
    <div>
      <Alert color="danger">페이지가 존재하지 않습니다.</Alert>
    </div>
  );
};

export default PageNotFound;
